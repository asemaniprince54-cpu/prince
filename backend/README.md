# SMS Notification App

This is an SMS notification app that allows you to send SMS messages using Twilio.

## Installation

1. Clone the repository.
2. Install dependencies using `npm install`.
3. Create a `.env` file and set your Twilio credentials.

## Usage

Run the server using `node src/index.js`.